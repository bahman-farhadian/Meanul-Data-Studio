"""h-bootstrap: prepares the stack so the services have something to work on.

Run once, as the last container to start. See ../README.md for the ordered
list of what it does and why each step is safe to repeat.
"""

# o-service-archiver — this piece's own operational targets.
#
# Included by the root Makefile (`include o-service-archiver/Makefile`), so
# every target here runs from the repository root, against the shared
# master .env — never `cd o-service-archiver && make`. What this component
# is and how to verify it: README.md.
#
# Nothing here yet: this service has no randomness of its own and touches
# only PostgreSQL - a prune count over time is the only thing worth
# watching, and README.md's own verify block already covers it.

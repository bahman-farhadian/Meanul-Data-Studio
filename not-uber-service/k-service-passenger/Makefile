# k-service-passenger — this piece's own operational targets.
#
# Included by the root Makefile (`include k-service-passenger/Makefile`), so
# every target here runs from the repository root, against the shared
# master .env — never `cd k-service-passenger && make`. What this component
# is and how to verify it: README.md.
#
# Nothing here yet. This is the natural home for tuning demand.py's own
# randomness — the request curve, TRIP_REQUESTS_PER_MINUTE pacing — once
# that work starts.

# m-service-city — this piece's own operational targets.
#
# Included by the root Makefile (`include m-service-city/Makefile`), so
# every target here runs from the repository root, against the shared
# master .env — never `cd m-service-city && make`. What this component is
# and how to verify it: README.md.
#
# Nothing here yet. This is the natural home for tuning counters.py — the
# per-zone hotspot score and how it feeds the price multiplier — once that
# work starts. `make profile` (root Makefile) is what first showed
# hotspot_score sitting at 100% NULL.

# l-service-dispatch — this piece's own operational targets.
#
# Included by the root Makefile (`include l-service-dispatch/Makefile`), so
# every target here runs from the repository root, against the shared
# master .env — never `cd l-service-dispatch && make`. What this component
# is and how to verify it: README.md.
#
# Nothing here yet. This is the natural home for tuning pricing.py/
# routing.py — the price-multiplier curve, the predicted-vs-actual duration
# bias, whether "no driver available" responds to real demand — once that
# work starts. `make profile` (root Makefile) is what first surfaced that
# this needs tuning.

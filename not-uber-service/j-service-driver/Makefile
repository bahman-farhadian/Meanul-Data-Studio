# j-service-driver — this piece's own operational targets.
#
# Included by the root Makefile (`include j-service-driver/Makefile`), so
# every target here runs from the repository root, against the shared
# master .env — never `cd j-service-driver && make`. What this component is
# and how to verify it: README.md.
#
# Nothing here yet. This is the natural home for tuning fleet.py's own
# randomness — movement, status transitions, DRIVER_TICK_SECONDS pacing —
# once that work starts.
